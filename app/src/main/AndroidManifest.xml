<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.fingerprint">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Transaction1Activity"
            android:label="@string/title_activity_transaction11"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Transaction2Activity"
            android:label="@string/title_activity_transaction21"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Register1Activity"
            android:label="Register Account"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".FingerprintActivity"
            android:label="Scan Fingerprint"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home1"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>